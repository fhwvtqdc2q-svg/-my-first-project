# دليل التشغيل المحلي

## الهدف

هذا الدليل يشرح طريقة تشغيل أول سكربت عملي في المشروع لتوليد تقرير نهاية اليوم من ملفات CSV محلية.

السكربت يعمل محليًا فقط ولا يقوم بأي اتصال خارجي أو إرسال رسائل أو تعديل بيانات المصدر.

## المتطلبات

- Python 3.9 أو أحدث.
- وجود ملفات البيانات داخل مجلد `samples/`.
- وجود قالب التقرير داخل `templates/daily-report.md`.

## تشغيل تقرير نهاية اليوم

من داخل مجلد المشروع، شغّل الأمر التالي:

```bash
python scripts/generate_daily_report.py --date 2026-05-12
```

## النتيجة

سيتم إنشاء ملف تقرير داخل مجلد `output/` باسم مشابه:

```text
output/daily-report-2026-05-12.md
```

## ماذا يفعل السكربت؟

- يقرأ المنتجات من `samples/products.csv`.
- يقرأ المخزون من `samples/inventory.csv`.
- يقرأ الزبائن من `samples/customers.csv`.
- يقرأ الفواتير من `samples/invoices.csv`.
- يقرأ المدفوعات من `samples/payments.csv`.
- يحسب تحصيل الكاش اليومي.
- يقارن الكاش بمتوسط آخر 7 أيام.
- يستخرج الزبائن المتأخرين 4 أيام أو أكثر.
- يستخرج الأصناف غير المتوفرة.
- يستخرج الأصناف المفعلة التي لا تحتوي على سعر.
- يولد تقرير Markdown.

## ما لا يفعله السكربت

- لا يرسل رسائل واتساب.
- لا يتصل بالإنترنت.
- لا يدخل إلى أي حساب.
- لا يغير ملفات البيانات الأصلية.
- لا يطبع فواتير.
- لا يرفع أي ملف إلى iCloud أو أي خدمة خارجية.

## مثال تشغيل بتاريخ اليوم تلقائيًا

```bash
python scripts/generate_daily_report.py
```

## ملاحظة أمان

هذا السكربت مخصص كبداية محلية آمنة. قبل استخدام بيانات حقيقية، يجب تجربة السكربت على بيانات وهمية والتأكد من أن التقرير يعطي نتائج صحيحة.
