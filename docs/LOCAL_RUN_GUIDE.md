# دليل التشغيل المحلي

## الهدف

هذا الدليل يشرح طريقة تشغيل السكربتات العملية الأولى في المشروع لتوليد تقارير ونشرات من ملفات CSV محلية.

السكربتات تعمل محليًا فقط ولا تقوم بأي اتصال خارجي أو إرسال رسائل أو تعديل بيانات المصدر.

## المتطلبات

- Python 3.9 أو أحدث.
- وجود ملفات البيانات داخل مجلد `samples/`.
- وجود القوالب داخل مجلد `templates/`.

## تشغيل تقرير نهاية اليوم

من داخل مجلد المشروع، شغّل الأمر التالي:

```bash
python scripts/generate_daily_report.py --date 2026-05-12
```

## نتيجة تقرير نهاية اليوم

سيتم إنشاء ملف تقرير داخل مجلد `output/` باسم مشابه:

```text
output/daily-report-2026-05-12.md
```

## ماذا يفعل سكربت التقرير؟

- يقرأ المنتجات من `samples/products.csv`.
- يقرأ المخزون من `samples/inventory.csv`.
- يقرأ الزبائن من `samples/customers.csv`.
- يقرأ الفواتير من `samples/invoices.csv`.
- يقرأ المدفوعات من `samples/payments.csv`.
- يحسب تحصيل الكاش اليومي.
- يقارن الكاش بمتوسط آخر 7 أيام.
- يستخرج الزبائن المتأخرين 4 أيام أو أكثر.
- يستخرج الأصناف غير المتوفرة.
- يستخرج الأصناف المفعلة التي لا تحتوي على سعر.
- يولد تقرير Markdown.

## تشغيل نشرة الأسعار اليومية

يمكن تشغيل مولد نشرة الأسعار مع إدخال سعر الصرف مباشرة:

```bash
python scripts/generate_price_list.py --date 2026-05-12 --exchange-rate 15000
```

أو بدون تمرير سعر الصرف، وسيطلبه السكربت يدويًا:

```bash
python scripts/generate_price_list.py --date 2026-05-12
```

## نتيجة نشرة الأسعار

سيتم إنشاء ملف داخل مجلد `output/` باسم مشابه:

```text
output/price-list-2026-05-12.md
```

## ماذا يفعل سكربت نشرة الأسعار؟

- يقرأ المنتجات من `samples/products.csv`.
- يقرأ المخزون من `samples/inventory.csv`.
- يطلب سعر الصرف يدويًا أو يستقبله من الأمر.
- يستبعد الأصناف غير المتوفرة.
- يستبعد الأصناف غير المفعلة.
- يضع الأصناف المتوفرة التي لا تحتوي على سعر في قائمة مراجعة.
- يولد نشرة أسعار بالدولار والليرة السورية.
- يضيف تقرير مراجعة قبل النشر.

## ما لا تفعله السكربتات

- لا ترسل رسائل واتساب.
- لا تتصل بالإنترنت.
- لا تجلب سعر الصرف تلقائيًا.
- لا تدخل إلى أي حساب.
- لا تغيّر ملفات البيانات الأصلية.
- لا تطبع فواتير.
- لا ترفع أي ملف إلى iCloud أو أي خدمة خارجية.
- لا تنشر على السوشيال ميديا.

## مثال تشغيل بتاريخ اليوم تلقائيًا

```bash
python scripts/generate_daily_report.py
python scripts/generate_price_list.py --exchange-rate 15000
```

## ملاحظة أمان

هذه السكربتات مخصصة كبداية محلية آمنة. قبل استخدام بيانات حقيقية، يجب تجربتها على بيانات وهمية والتأكد من أن النتائج صحيحة.
